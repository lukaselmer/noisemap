language: ruby
rvm:
  - "2.0.0"
before_install:
  - gem install bundler
  - rm Gemfile.lock
  - bundle update
before_script:
  - cp config/database.travis.yml config/database.yml
  - psql -c 'create database noisemap_test;' -U postgres
  